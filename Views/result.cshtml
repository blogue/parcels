<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>Mad Libs</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <h1>The calculate cost:</h1>
    <p>Height: @Model.GetHeight() <br>
       Width: @Model.GetWidth() <br>
       Depth: @Model.GetDepth() <br>
       Volume: @Model.Volume() <br>
      Cost: $ @Model.Cost() </p>
  </body>
</html>
